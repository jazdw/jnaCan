# override this by putting a jnaCan-test.properties file on your classpath

can.receiveTimeout=200
can.txInterface=vcan0
can.txInterface.mtu=16
can.rxInterface=vcan0
can.rxInterface.mtu=16

test.timeout=10000

test.txRx.numPackets=1000
